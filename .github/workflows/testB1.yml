name: demo-summary-basic

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Écrire un résumé
        run: |
          echo "## Résumé du workflow" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Tout s'est bien passé." >> $GITHUB_STEP_SUMMARY